package com.construction.material.entity;

/**
 * Closed catalog of licensable feature modules. Single source of truth —
 * no other list of module names should exist anywhere else in the codebase.
 */
public enum LicenseModule {
    PROJECTS,
    MATERIALS,
    ORDERS,
    USAGE,
    STOCK,
    ADMIN
}
